# This file for test code snippets.

# Ask user for their name and greet them
name = input("what's your name? ")

# remove leading/trailing whitespace
name = name.strip()

# Capitalize the first letter of the
name = name.capitalize()

# Alternative: Capitalize the first letter of each word
name = name.title()

# Greet the user
print(f"hello, {name}")

# Import necessary libraries
array_reshape(x, dim, order = c("C", "F")): Reshape a Python array.name)

# Split the name into parts (first and last)
first, last = name.split(" ")dddd